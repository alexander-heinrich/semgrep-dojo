rules:
  - id: weak-hash-algorithm
    message: MD5 and SHA-1 are broken for security purposes; prefer SHA-256 or better
    languages: [csharp]
    severity: WARNING
    pattern-either:
      - pattern: MD5.Create()
      - pattern: SHA1.Create()
