rules:
  - id: redirect-of-parameter
    message: $M redirects to its own parameter $P without validation
    languages: [csharp]
    severity: WARNING
    patterns:
      - pattern: Redirect($P)
      - pattern-inside: |
          $R $M(..., string $P, ...) { ... }
