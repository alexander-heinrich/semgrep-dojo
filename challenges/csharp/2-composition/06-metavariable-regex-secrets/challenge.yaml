title: metavariable-regex — a regex on what a metavariable captured
level: 2
difficulty: 3
rule_id: hardcoded-secret
tags: [metavariable-regex, security, string-literal]
covers:
  cheatsheet: [argument]
  rule_keys: [metavariable-regex, patterns]
wasm: cli-only
instructions: |
  This model class from a deliberately vulnerable API (dvcsharp-api) declares four string
  constants. Three are harmless role names; the fourth is a 64-character hex string used to sign JWTs
  — a hard-coded secret.

  `public const string $N = $V;` captures every constant. To keep only the secret we need to look
  *at the text* that `$V` captured. That is what `metavariable-regex` does: inside a `patterns:`
  block it filters matches by running a regular expression over the source text bound to a
  metavariable.

  Add a `metavariable-regex` entry that keeps only values consisting of 32 or more hex digits.
  Remember that `$V` captured the literal *including its quotes*.
hints:
  - "The entry looks like `- metavariable-regex: {metavariable: $V, regex: ...}` and sits under `patterns:` next to the `- pattern:` line."
  - "Anchor the regex and include the quotes: `^\"[0-9a-f]{32,}\"$`."
followup_title: Correct!
followup: |
  `metavariable-regex` sees the raw source text of the binding — for a string literal that is
  `"f449a71c…"`, quotes included, which is why the regex starts with `^"`. Without anchors,
  `[0-9a-f]{32,}` would still work here, but `^…$` makes the intent explicit and avoids matching a
  long hex string embedded in a longer literal.

  The regex operators are the one family of features the browser engine on this site cannot run
  (it has no regex library), which is why this challenge is checked with the real Semgrep CLI.

  **Try this:** run `metavariable-regex` on `$N` instead, with `regex: (?i)secret|token|password` — a
  common way to flag suspicious *names* rather than values. Combine both with two entries under
  `patterns:` and see that they are ANDed.
sources:
  - repo: appsecco/dvcsharp-api
    path: Models/User.cs
    commit: 76c1de3c9d8d9c2e8ec0b50abe3b198a4330d7fc
    license: MIT
    copyright: "2022 Appsecco Ltd."
    url: https://github.com/appsecco/dvcsharp-api/blob/76c1de3c9d8d9c2e8ec0b50abe3b198a4330d7fc/Models/User.cs
    lines: [4, 28]
    modified: true
    modification_note: "Members after the constants replaced by `// ... (omitted)`; annotation comments added."
